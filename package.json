{
  "name": "viralix-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "heroku-prebuild": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
